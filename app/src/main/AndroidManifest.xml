<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.com.misa.CUKCUKLite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".order.addFood.selectUnit.FormSelectUnit"></activity>
        <activity android:name=".order.editFood.FormEditFood" />
        <activity android:name=".login.loginPhoneNumber.LoginPhoneMainView" />
        <activity android:name=".SplashApp" >

        </activity>

        <activity android:name=".order.addFood.FormAddFood" />
        <activity android:name=".login.LoginMain" />
        <activity
            android:name=".MainApp"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>